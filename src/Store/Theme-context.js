import React from "react";
import { useState } from "react";

const Themecontext = React.createContext({});
export default Themecontext;
// { light:'linear-gradient(to right, #5800ff, #00d7ff, #5800ff)' , dark: '' }
